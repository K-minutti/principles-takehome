import React from "react";

const BreedListFilter = () => {
  return (
    <form>
      <input
        type="text"
        id="searchFilter"
        placeholder="Filter list by name, country..."
      />
    </form>
  );
};

export default BreedListFilter;
